#include <stdio.h>

int main() {
    double a, z1, z2;
    scanf("%lf", &a);
    z1 = 1 / (((1 + a + a * a) / (2 * a + a * a) + 2 - (1 - a + a * a) / (2 * a - a * a))) * (5 - 2 * a * a);
    z2 = (4 - a * a) / 2;
    printf("%lf\n%lf", z1, z2); 
    return 0;

}
