#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {

    freopen(argv[1], "w + b", stdout);
    int x = atoi(argv[2]), y = atoi(argv[3]);
    printf("%d", x + y);

    return 0;
}
